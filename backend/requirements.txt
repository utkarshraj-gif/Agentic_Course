# Core (everything runs offline with just these)
pydantic>=2.7
numpy>=1.26
scikit-learn>=1.4
langgraph>=0.3
langchain-core>=0.3
faiss-cpu>=1.8
rank-bm25>=0.2.2
chromadb>=0.5
mcp>=1.2
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
markdown>=3.6
pytest>=8

# Online mode (real models): set OPENAI_API_KEY; optional LLM_BASE_URL for vLLM / Azure OpenAI
openai>=1.40

# Optional integrations - install when you reach the class that uses them
# langchain-openai        # Class 1-2 LangChain chat model wrappers
# dspy                    # Class 2 prompt programs + optimisers
# redis                   # Class 6 long-term memory backend (REDIS_URL)
# langmem                 # Class 6 managed memory extraction
# langsmith               # Class 7/11 tracing + evaluation (LANGSMITH_API_KEY)
# opik                    # Class 7/11 tracing + evaluation (OPIK_API_KEY)
# vllm                    # Class 10 serving (GPU host)
# opentelemetry-sdk opentelemetry-exporter-otlp   # Class 11
